import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.seantara.musictuner',
  appName: 'Music Tuner Pro',
  webDir: 'dist'
};

export default config;
